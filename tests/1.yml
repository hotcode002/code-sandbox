config:
    target: "http://35.244.51.107"
    phases:
        - duration: 60
          arrivalRate: 10
    defaults:
        headers:
            Authorization: "Bearer 2AEBEDD97C13DAD53F4B22345457F"
scenarios:
    - flow:
          - post:
                url: "/submissions"
                json:
                    language: "C"
                    source_code: "#include <stdio.h> \n int main() \n { \n printf(\"Hello, World!\"); \n return 0; \n }"
                capture:
                    - json: "$"
                      as: body
                log: "{{body}}"
